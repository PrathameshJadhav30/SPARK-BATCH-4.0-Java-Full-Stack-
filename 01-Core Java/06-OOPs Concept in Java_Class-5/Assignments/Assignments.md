# 💻 Java OOPs Concepts - Assignment

**Name:** Prathamesh Jadhav  
**Branch:** Artificial Intelligence and Data Science Engineering

This assignment covers foundational Object-Oriented Programming concepts in Java using unique real-world examples.

---

## ✅ PART A: Class, Object, Constructor

### Q1. Class and Object  
Create a class `Laptop` with attributes `brand`, `price`, and `RAM`. Create an object and display the details.

### Q2. Constructor  
Create a class `Movie` with a constructor that initializes the movie name and duration. Display the movie details.

---

## ✅ PART B: Types of Constructor

### Q3. Default Constructor  
Create a class `Planet` that sets default values like `Earth` and its average temperature using a default constructor.

### Q4. Parameterized Constructor  
Create a class `LibraryBook` with a constructor that accepts the book's title, author, and ISBN. Print book details.

### Q5. Overloaded Constructor  
Create a class `MobilePhone` with two constructors – one with default brand and price, and another with values passed as parameters.

---

## ✅ PART C: `this` Keyword

### Q6. `this` Keyword  
Create a class `BankCustomer` that demonstrates the use of `this` keyword when constructor parameters shadow class variables.

---

## ✅ PART D: OOPs Principles

### Q7. Encapsulation  
Create a class `UserProfile` that hides `password` and `email` with private fields. Provide public setters and getters.

### Q8. Inheritance - Single  
Create a class `Vehicle` with method `start()`. Derive a class `Car` that adds `speedUp()` method.

### Q9. Polymorphism - Compile-Time  
Create a class `Printer` with multiple `print()` methods to print integers, strings, and doubles.

### Q10. Abstraction  
Create an abstract class `Appliance` with an abstract method `turnOn()`. Inherit it in a class `WashingMachine`.

---

## ✅ PART E: Overriding

### Q11. Method Overriding  
Create a class `Animal` with a method `makeSound()`. Override it in `Lion` class to print a lion's roar.

---

## ✅ PART F: Types of Inheritance

### Q12. Multilevel Inheritance  
Create classes: `Organism` → `Animal` → `Human`. Demonstrate inherited methods across all levels.

### Q13. Hierarchical Inheritance  
Create base class `Employee`, and subclasses `Manager` and `Developer` with individual methods.

---

## ✅ PART G: `super` Keyword

### Q14. Use of `super`  
Create a class `Shape` with a constructor and a method. In subclass `Rectangle`, use `super` to call parent constructor and method.

---

## ✅ PART H: Polymorphism

### Q15. Compile-time Polymorphism  
Create a class `MathOperations` with overloaded methods `multiply()` that handle int, double, and three parameters.

### Q16. Run-time Polymorphism  
Create a parent class `Bird` with method `fly()`. Override it in class `Eagle`. Call `fly()` using parent reference.

---

## ✅ PART I: Dynamic Method Dispatch

### Q17. Dynamic Dispatch  
Create a base class `Notification` with method `alert()`. Derive classes `EmailNotification` and `SMSNotification`. Call `alert()` using dynamic dispatch.

---


