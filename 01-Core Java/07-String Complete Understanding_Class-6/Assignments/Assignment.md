# Java String Concepts – Assignment & Coding Round Questions

**Name**: Prathamesh Jadhav  
**Branch**: Artificial Intelligence and Data Science Engineering  

---

## 📘 Part A: Core Java String Assignment

### 1. String Object Creation
- **Q1**: Write a program to create strings using literals and using the `new` keyword. Print whether the objects are pointing to the same memory or not using `==` and `.equals()`.

### 2. String Pool vs Heap
- **Q2**: Create a program that demonstrates the difference between string pool (constant pool) and heap memory with examples.

### 3. String Immutability
- **Q3**: Demonstrate with an example why strings are immutable. Try changing a string without assignment and observe the result.

### 4. StringBuffer and StringBuilder
- **Q4**: Write two separate programs using `StringBuffer` and `StringBuilder` to append and insert text into a string. Show how they differ from `String`.

### 5. Comparison Between String Types
- **Q5**: Create a program that compares `String`, `StringBuffer`, and `StringBuilder` on the basis of mutability and performance.

### 6. String Methods
- **Q6**: Write a Java program using various `String` methods like `charAt()`, `substring()`, `replace()`, `trim()`, `toUpperCase()`, `toLowerCase()`.

---

## 💻 Part B: Company Coding Round String Questions

### Easy-Level:

1. **Reverse a String**  
   - Input: `"hello"` → Output: `"olleh"`

2. **Check if a String is Palindrome**  
   - Input: `"madam"` → Output: `true`

3. **Count vowels and consonants in a string**  
   - Input: `"hello"` → Output: Vowels = 2, Consonants = 3

4. **Remove all duplicates from a string**  
   - Input: `"programming"` → Output: `"progamin"`

---

### Medium-Level:

5. **Longest Substring Without Repeating Characters**  
   - Input: `"abcabcbb"` → Output: `"abc"`

6. **Check if two strings are anagrams**  
   - Input: `"listen", "silent"` → Output: `true`

7. **Compress a string using counts of repeated characters**  
   - Input: `"aabcccccaaa"` → Output: `"a2b1c5a3"`

8. **Find the first non-repeating character in a string**  
   - Input: `"swiss"` → Output: `"w"`

---

### Hard-Level:

9. **Minimum Window Substring (Leetcode)**  
   - Given strings `s` and `t`, return the minimum window in `s` which will contain all the characters in `t`.

10. **Zigzag Conversion of String**  
   - Given a string and number of rows, print the zigzag form.

11. **Wildcard Pattern Matching**  
   - Supports `*` and `?` in pattern matching.

---

> 💡 These problems are frequently asked by companies like **Amazon**, **Google**, **Infosys**, **TCS**, **Capgemini**, **Wipro**, and **Cognizant**.
