# Java Assignment Questions

## 🧮 A. Operators - Assignment Questions

### 1. Basic Arithmetic Calculator

Write a Java program to perform addition, subtraction, multiplication, division, and modulus on two numbers entered by the user.

### 2. Check Odd or Even using Modulus Operator

Input a number and check whether it's odd or even using the `%` operator.

### 3. Area Calculator using Arithmetic Operators

Calculate the area of:

* Circle: $\pi r^2$
* Rectangle: $l \times b$
* Triangle: $\frac{1}{2} \times b \times h$

### 4. Demonstrate Assignment Operators

Take a number and show the effect of `+=`, `-=`, `*=`, `/=`, `%=` one by one.

### 5. Bitwise Operator Demo

Perform bitwise AND, OR, XOR, NOT, left shift and right shift on two integers.

### 6. Unary Operator Logic

Show the difference between prefix (`++x`) and postfix (`x++`) increments using a small program.

---

## ✅ B. Conditional Statements - Assignment Questions

### 1. Find the Largest of Two Numbers using if-else

Take two numbers and find which one is greater.

### 2. Check if a Number is Positive, Negative, or Zero

Use `if-else-if` to check a number’s sign.

### 3. Grade Calculator

Take marks as input (0–100) and print grade:

* 90–100: A
* 80–89: B
* 70–79: C
* 60–69: D
* Below 60: F

### 4. Eligibility for Voting (Nested if)

Ask for `age` and `hasID`:

* If age ≥ 18 and has ID → "Allowed"
* Else → "Denied"

### 5. Leap Year Checker

Use conditions to check if a given year is a leap year or not.

---

## 🎯 C. Switch-Case - Assignment Questions

### 1. Simple Calculator using Switch

Take two numbers and an operator (`+`, `-`, `*`, `/`, `%`) as input and perform operation.

### 2. Day of the Week

Take number (1–7) and print the corresponding day using switch:

* 1 → Monday, 2 → Tuesday, ..., 7 → Sunday

### 3. Month Days Checker

Input month number (1–12), print number of days in the month (use switch).

### 4. Vowel or Consonant

Input a character and check whether it is a vowel or consonant using switch-case.

### 5. Traffic Signal Simulation

Input signal color (`red`, `yellow`, `green`) and print corresponding message using switch.

---

## 💡 Bonus Challenge (Mixed Concepts)

### 1. Electricity Bill Generator

Use `if-else` and operators:

* Units ≤ 100 → ₹1.5/unit
* 101–200 → ₹2.5/unit
* > 200 → ₹3.5/unit

Also apply:

* Fixed charge: ₹50
* If total bill > ₹500, add 10% surcharge
