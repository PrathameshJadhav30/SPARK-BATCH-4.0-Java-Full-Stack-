let result = 5 + 3 * 2;      // 11
let changed = (5 + 3) * 2;   // 16
console.log(result, changed);
let complex = 5 + 3 * 2 - 4 / 2; // 10
console.log(complex); // 10
//Note: Parentheses can change the order of operations
