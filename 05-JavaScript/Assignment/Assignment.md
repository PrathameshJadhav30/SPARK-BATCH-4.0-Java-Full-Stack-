# 📘 JavaScript Programming Assignments  
By: **Prathamesh Jadhav**  
Branch: **Artificial Intelligence and Data Science Engineering**

---

## ✅ 1. What is JavaScript?
- Q1. Write a JavaScript program that prints "Hello JavaScript" to the console.

---

## ✅ 2. Why JavaScript?
- Q2. Write 3 real-world use cases of JavaScript in a web application as comments.

---

## ✅ 3. Evolution of JavaScript
- Q3. List down 3 important features introduced in ES6 and write comments explaining them.

---

## ✅ 4. Use-cases of JavaScript
- Q4. Mention 2 frontend and 2 backend use-cases of JavaScript in the form of comments.

---

## ✅ 5. Console Methods
- Q5. Demonstrate the usage of `console.log`, `console.warn`, `console.error`, and `console.debug` with examples.

---

## ✅ 6. Alerts & Prompts
- Q6. Show an alert message.
- Q7. Take user input using `prompt` and print it in the console.

---

## ✅ 7. Variables in JavaScript
- Q8. Declare and initialize variables using `var`, `let`, and `const`.
- Q9. Show reassignment and scope difference using `var`, `let`, and `const`.

---

## ✅ 8. Data Types
- Q10. Create variables of all JavaScript data types: string, number, boolean, null, undefined, object, symbol, and bigint.

---

## ✅ 9. Primitive vs Reference
- Q11. Show the difference between primitive and reference data types through a small program.

---

## ✅ 10. Conditional Statements
- Q12. Write a program using `if-else` to print grade based on marks.
- Q13. Use `switch` to display the day of the week based on a number input.

---

## ✅ 11. Loops
- Q14. Print numbers from 1 to 10 using `for`, `while`, and `do-while` loops.
- Q15. Use `break` and `continue` statements inside a loop.

---

## ✅ 12. Functions
- Q16. Write a function to add two numbers (normal function).
- Q17. Create the same using function expression and arrow function.
- Q18. Write a function with default parameters.
- Q19. Demonstrate callback function using a simple example.

---

## ✅ 13. Arrays
- Q20. Create an array and perform push, pop, shift, and unshift operations.
- Q21. Use `for`, `for...of`, and `forEach` loops to print elements.
- Q22. Use `map`, `filter`, and `reduce` on an array.
- Q23. Use `slice`, `splice`, `indexOf`, and `includes`.

---

## ✅ 14. Strings
- Q24. Create a string and perform slicing, splitting, replacing, and changing case.
- Q25. Use template literals to display a personalized message.

---

## ✅ 15. Objects
- Q26. Create an object with properties and methods.
- Q27. Access and update object values using dot and bracket notation.
- Q28. Loop through the object using `for...in`.
- Q29. Use `Object.keys()`, `values()`, and `entries()` on an object.

---

## ✅ 16. `this` Keyword
- Q30. Demonstrate the usage of `this` inside an object method.
- Q31. Show how `this` behaves in arrow functions vs normal functions.

---

## ✅ 17. Constructor Function
- Q32. Create a constructor function for `Student` with properties like name, roll number, and course.
- Q33. Create multiple student objects using the constructor.

---

## ✅ 18. Event Listener
- Q34. Create a button in HTML and show an alert when it is clicked using `addEventListener`.

---

## ✅ 19. Call, Apply, Bind
- Q35. Create a function and use `call`, `apply`, and `bind` to change its context to another object.

---

## ✅ 20. Closure
- Q36. Write a function that returns a counter function using closure.
- Q37. Show that the counter retains its value on every call.

---

## ✅ 21. DOM Manipulation
- Q38. Select an element using `getElementById` and change its text and style.
- Q39. Add and remove a class using `classList`.
- Q40. Create and delete an element dynamically.

---

## ✅ 22. BOM (Browser Object Model)
- Q41. Print the window's inner width and height.
- Q42. Display the current URL using `location.href`.
- Q43. Use `setTimeout()` to show a delayed message.

---

## ✅ 23. Event Handling
- Q44. Add click and mouseover events to a div.
- Q45. Change the background color of an element on button click.

---

## ✅ 24. Fetch API
- Q46. Fetch data from `https://jsonplaceholder.typicode.com/posts/1` and print the title.
- Q47. Handle error in the fetch request using `.catch()`.

---

